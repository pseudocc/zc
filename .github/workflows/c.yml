name: C CI

on:
  push:
    branches: [ aloof ]
  pull_request:
    branches: [ aloof ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: make
      run: make
    - name: ztest
      run: ./ztest
